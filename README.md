# tree-sitter-toolang

Minimal Tree-sitter grammar for Toolang.

This first version is intentionally line-oriented. It is designed to support
useful highlighting in editors before the grammar becomes structurally complete.

Generate parser files with:

```bash
npm install
npx tree-sitter generate
```
